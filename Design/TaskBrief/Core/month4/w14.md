# Task Brief Week 14 - Core Team

**Module target:** `src/app/**`, `src/components/**`, `src/modules/**/[Pp]anel|[Vv]iew*.tsx` — **UI Adoption & Polish**: thay primitive W13 vào App Shell + panel Write/Check/Export/Submission/Evidence/Present (không đổi behavior), áp Patterns (empty/loading/error/validation/feedback) + microcopy + responsive + dark-mode polish, và Visual QA theo Art Direction (RoadMap Phase 4 / W14). `src/modules/format/**` hiện logic-only, không có panel/view để adopt.
**Git branch:** `feature/W14-ui-adoption`

> **Cross-cutting constraints.** All types conform to `Design/Modules/Other/CanonicalTypes.md` (single source — never re-declare a shape). `tsconfig` `strict: true`, no `any`. Route `/` stays the workspace editor. ≤200 lines/file (`VibeCode.md` §3); inter-module imports only via `index.ts`; CSS uses only `var(--rs-*)` tokens.
>
> **🎨 Frontend discipline.** Adoption bám `Design/Frontend/4.Layouts/*` (AppShell/Responsive/IA), `5.Flows/*` (Write/Export/Present), `3.Patterns/*` (empty/loading/error/validation/feedback). Visual gate = `0.ArtDirection.md` §11 (6 câu) + anti-patterns §6.
>
> **W14 install matrix.** **KHÔNG cài lib mới** — chỉ dùng primitive W13 (`src/components/ui/`) + token. Bất kỳ `npm install` runtime nào tuần này = vi phạm (`TechnicalStack §8c`).
>
> **🔒 Trình bày, không đổi behavior/shape (`WorkFlow.md` DoD).** W14 thay UI + chuẩn hoá states; **không** thêm logic nghiệp vụ, **không** đổi `CanonicalTypes`, **không** đụng pipeline/checker/export logic. Chỉ sửa logic khi adoption lộ lỗi thật — fix tối thiểu.
>
> **🔒 Report Output bất biến.** Chỉ polish **Workspace UI**. Tờ báo cáo (`--rs-report-*`, preview A4) **không** đổi — giữ deterministic preview ↔ PDF ↔ DOCX (`0.ArtDirection.md` §2, `DesignSystem_Tokens.md §7`).

> **⚠️ Reconciliation — không trùng W12 polish.** W12 đã chuẩn hoá **shared states baseline** `src/components/states/` (`w12.md` C130/C131) + a11y **checklist thủ công** (C132/C133). W14 **nâng cấp** dùng primitive/pattern mới (Toast/Dialog/EmptyState thật) chứ không lặp lại. A11y tuần này là **checklist thủ công**; **axe tự động hoá để W15** (`w15a`), không tuyên bố "axe gate" ở W14.
>
> **⚠️ Reconciliation — QA nặng dời sang W15.** Visual QA toàn màn + axe automation + **before/after evidence** (light/dark × ≥3 viewport) + verify dark/motion/responsive toàn diện thuộc **W15** (`w15b`–`w15e`). W14 chỉ self-check theo tuần + adoption coverage report; **đóng Phase 4 ở W15**, không phải W14.
>
> **⚠️ Reconciliation — Tabs vào panel phải.** Check/Export/Submission/Evidence/Present chuyển dùng `Tabs` (underline) + count badge số issue (`Frontend/2.Components/Tabs.md`, B11). SubmissionPanel và EvidencePanel là live panels trong `Workspace.tsx`, không được bỏ khỏi adoption.
>
> **⚠️ Reconciliation — dark mode chỉ vỏ UI.** Override token UI ở `[data-theme="dark"]` (`DesignSystem_Tokens.md §2.5`); severity-bg bắt buộc override; `--rs-report-*` không đổi.

## Day 1: App Shell + Layout Adoption

- `[C146]` `[MODIFY]` `src/app/layout.tsx` + `WorkspaceLayout.tsx` theo `Frontend/4.Layouts/AppShell.md`: toolbar sticky, hệ pane editor↔preview, border mảnh chia vùng, một primary action. Thêm `ToastProvider`/Viewport (dưới-phải) ở root. Không đổi behavior layout.
- `[C147]` Implement `Frontend/4.Layouts/Responsive.md`: breakpoint editor↔preview (split → toggle/tab khi hẹp), panel phụ thành drawer; tờ A4 scale-to-fit không méo. Verify ≥3 viewport.

## Day 2: Write Panels + Core Patterns (Format logic-only)

- `[C148]` `[MODIFY]` Write/metadata/template surfaces dùng `Input`/`Textarea`/`Select`/`Button`; áp `FormValidation` (blur/submit, aria) + `Feedback` (autosave quiet, `5.Flows/Write.md`).
- `[C149]` Implement shared patterns `Frontend/3.Patterns/`: `EmptyState`/`LoadingSkeleton`/`ErrorState` (tái dùng `src/components/states/` từ W12 nếu có, nâng cấp). Áp cho: chưa có report, report trống, preview đang render. Copy từ `VoiceAndContent.md §5`.
  - Format note: `src/modules/format/**` chỉ có logic/test (`number-headings`, `generate-toc`, `captions`, ...), không có panel/view `.tsx`; không adopt UI ở Day 2 trừ khi phát hiện surface thật.

## Day 3: Check/Export/Submission/Evidence/Present Panels + Tabs

- `[C150]` `[MODIFY]` Panel phải dùng `Tabs` (Check/Export/Submission/Evidence/Present) + count badge số issue; Checker dùng `Badge` severity (icon+chữ) + clickable jump-to-issue; readiness badge theo ngưỡng.
- `[C151]` `[MODIFY]` Export/Present dùng `Button` (loading state), `Dialog` confirm (xuất khi còn lỗi — `VoiceAndContent.md §7`), `Toast` (export done + action "Mở file"), error+retry (`3.Patterns/ErrorStates.md`, `5.Flows/Export.md`). Submission/Evidence adopt primitive/form/state chung (`SubmissionPanel.tsx`, `EvidencePanel.tsx`, `EvidenceForm.tsx`) và giữ nguyên logic/reducer. Nút PPTX giữ disabled + ghi chú (deferred parity W12).

## Day 4: Microcopy · Dark Mode · Motion Polish

- `[C152]` Áp microcopy toàn UI theo `Frontend/Other/VoiceAndContent.md`: nhãn nút = động từ, message lỗi giúp-đỡ, empty-state copy, toast/confirm. Việt 100% (giữ Markdown/PDF/DOCX/HTML/TOC). Không emoji trong copy vận hành.
- `[C153]` Dark mode polish (`DesignSystem_Tokens.md §2.5`) + motion ≤200ms/`prefers-reduced-motion` (`Frontend/Other/Motion.md`). Verify severity-bg override; `--rs-report-*` bất biến khi dark.

## Day 5: Visual QA Gate & Before/After Evidence

- `[C154]` Self Visual QA các màn vừa adopt theo `0.ArtDirection.md` §11 (6 câu) + anti-patterns §6 (một primary/màn, đủ trạng thái, lấy từ component chung) + a11y **checklist thủ công**; run lint/typecheck/test/build. *(Axe automation + Visual QA toàn diện → W15 `w15a`/`w15b`.)*
- `[C155]` Write `Design/Reports/Month4/W14/W14_QA_Report.md` (map DoD + adoption coverage Pattern/Layout/Flow) + screenshots cơ bản (light/dark) các màn vừa adopt + `build_output.txt`. *(Before/after evidence đầy đủ ≥3 viewport + cập nhật `Frontend/` "implemented" + đóng Phase 4 → W15 `w15e`.)*

## W14 Locked Decisions (review-agreed)

1. **Adoption, không đổi behavior/shape.** Thay UI + chuẩn hoá states; không thêm feature/đổi CanonicalTypes/đụng pipeline; fix tối thiểu khi lộ lỗi (`WorkFlow.md` DoD).
2. **Không cài lib mới.** Chỉ dùng primitive W13 + token; `npm install` runtime tuần này = vi phạm.
3. **Report Output bất biến.** Chỉ polish Workspace UI; `--rs-report-*`/preview A4 giữ deterministic (`0.ArtDirection.md §2`).
4. **Tabs + Badge vào panel phải.** Check/Export/Submission/Evidence/Present dùng Tabs + count badge; severity icon+chữ; jump-to-issue clickable (B7/B11).
5. **Dark mode chỉ vỏ.** Override token UI; severity-bg override; report không đổi (`§2.5`).
6. **Giữ a11y baseline (W12 checklist).** A11y **checklist thủ công** sau adoption (keyboard/focus/ARIA), không hạ chuẩn; **axe tự động hoá ở W15** (`w15a`), không tuyên bố "axe gate" tuần này.

## Risks

| Risk | Level | Mitigation |
|---|---:|---|
| Adoption "lén" đổi behavior/logic | High | Chỉ MODIFY tầng trình bày; diff review; không đổi shape (Locked #1). |
| Làm vỡ deterministic preview ↔ PDF | High | Không đụng `--rs-report-*`/pipeline; verify export mẫu vẫn parity (Locked #3). |
| Trùng lặp / lặp lại W12 polish | Medium | Nâng cấp dùng primitive/pattern mới, không lặp; reconcile W12 (note). |
| Dark mode chói (severity-bg) | Medium | Bắt buộc override `§2.5`; verify cả hai theme (Locked #5). |
| A11y tụt sau khi đổi UI | Medium | A11y checklist thủ công (keyboard/focus/ARIA); giữ Radix primitive; axe tự động bắt phần còn lại ở W15 (Locked #6). |
| Responsive vỡ tờ A4 | Low | scale-to-fit không méo; test ≥3 viewport (C147). |
| File panel > 200 dòng sau adoption | Low | Tách subcomponent/`.css` co-located (`VibeCode §3`). |
