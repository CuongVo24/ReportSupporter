# Task Brief Week 15 - Core Team

**Module target:** `src/components/**`, `src/modules/**/[Pp]anel|[Vv]iew*.tsx`, `src/app/**` (verify/hardening, không thêm feature) — **UI Hardening, A11y Automation & Visual QA**: gánh phần QA/evidence nặng tách khỏi W13 (build) và W14 (adopt) để hai tuần đó không phải đánh đổi chất lượng. Dựng **axe automation** (gate a11y tự động thật), Visual QA toàn màn theo Art Direction, hardening dark/motion/responsive đa viewport, hardening edge-state các primitive/panel (buffer hấp thụ spillover Day 3 của W13/W14), và **before/after evidence** + đóng Phase 4 (RoadMap Phase 4 / W15).
**Git branch:** `feature/W15-ui-hardening`

> **Cross-cutting constraints.** All types conform to `Design/Modules/Other/CanonicalTypes.md` (single source — never re-declare a shape). `tsconfig` `strict: true`, no `any`. Route `/` stays the workspace editor. ≤200 lines/file (`VibeCode.md` §3); inter-module imports only via `index.ts`; CSS uses only `var(--rs-*)` tokens.
>
> **W15 install matrix.** Adds **dev-only** a11y test dep (exact pin, approve trước khi cài): `axe-core` (+ matcher `vitest-axe` nếu dùng) — chạy trong **Vitest + jsdom**, **không** kéo Playwright (vẫn Deferred `TechnicalStack §7`). **FORBIDDEN:** runtime dep mới; UI kit/Tailwind/CSS-in-JS/animation lib (`TechnicalStack §2b`); axe như runtime (chỉ devDependency).
>
> **🔒 Hardening, không đổi behavior/shape (`WorkFlow.md` DoD).** W15 verify + fix tối thiểu khi gate đỏ; **không** thêm logic nghiệp vụ, **không** đổi `CanonicalTypes`, **không** đụng pipeline/checker/export logic. Không redesign mới.
>
> **🔒 Report Output bất biến.** Chỉ verify/polish Workspace UI; `--rs-report-*`/preview A4 giữ deterministic (`0.ArtDirection.md §2`, `DesignSystem_Tokens.md §7`).

> **⚠️ Reconciliation — W15 gánh QA/evidence của W13/W14.** Axe automation + Visual QA toàn màn + before/after evidence (light/dark × ≥3 viewport) + verify dark/motion/responsive **dời từ W13 Day 5 / W14 Day 4–5 sang đây**. W13/W14 chỉ self-check + coverage report theo tuần; gate toàn diện + đóng Phase 4 thuộc W15.
>
> **⚠️ Reconciliation — axe lần đầu tự động ở đây.** W12 a11y là checklist thủ công (`w12.md` C132/C133); W13/W14 a11y cũng checklist thủ công khi build/adopt; **W15 mới dựng axe tự động** (`axe-core` + jsdom) và đẩy về **0 critical**. Mọi tham chiếu "axe gate" của Phase 4 hội tụ ở W15.
>
> **⚠️ Reconciliation — buffer cho Day 3 nặng.** Day 4 (C162/C163) là chỗ hấp thụ spillover: hoàn thiện edge-state Dialog/Toast/Tabs (W13 Day 3) + edge-state empty/loading/error panel Check/Export/Present (W14 Day 3) nếu hai tuần trước phải làm gấp.

## Day 1: A11y Automation (axe)

- `[C156]` Add **devDependency** `axe-core` (+ `vitest-axe` nếu dùng matcher), exact pin, `npm install --save-exact`; scaffold harness a11y trong Vitest + jsdom (`src/components/ui/__a11y__/` hoặc `*.a11y.test.tsx`). Không runtime dep.
- `[C157]` Chạy axe trên mọi primitive `src/components/ui/*` + shared states + màn chính; fix **tối thiểu** đến **0 critical** (không đổi shape/behavior). Ghi vi phạm + cách sửa.

## Day 2: Cross-screen Visual QA (Art Direction)

- `[C158]` Visual QA mọi màn chính (Write/Check/Export/Present/Shell) theo `0.ArtDirection.md §11` (6 câu) + anti-patterns §6: một primary/màn, màu "kiếm được chỗ", đủ trạng thái, lấy từ component chung.
- `[C159]` Fix tối thiểu các điểm trượt §11/§6 (không thêm feature/redesign); ghi pass/fix theo từng màn.

## Day 3: Dark / Motion / Responsive Hardening

- `[C160]` Verify **dark mode** mọi surface (token UI override `§2.5`, severity-bg không chói, focus ring rõ trên nền tối) + **motion** ≤200ms/`prefers-reduced-motion`; `--rs-report-*` bất biến khi dark. Fix tối thiểu.
- `[C161]` Verify **responsive** ≥3 viewport (split→toggle khi hẹp, panel phụ → drawer, tờ A4 scale-to-fit không méo). Fix tối thiểu layout.

## Day 4: Primitive / Panel Edge-state Hardening (buffer)

- `[C162]` Hoàn thiện **edge-state primitive** spillover W13 Day 3: Dialog (drawer/confirm focus-return), Toast (stack >3, error-no-dismiss, hover-pause), Tabs (count badge realtime) — đủ state theo `_ComponentSpecRule.md §3`.
- `[C163]` Hoàn thiện **edge-state panel** spillover W14 Day 3: empty/loading/error của Check/Export/Present (jump-to-issue rỗng, export fail+retry, present chưa có outline). Không đổi logic/shape.

## Day 5: Before/After Evidence & Phase 4 Close

- `[C164]` `[NEW]` `Design/Reports/Month4/W15/screenshots/` — **before/after** (light/dark × ≥3 viewport) các màn chính; chạy 4 gates + axe (0 critical, light+dark).
- `[C165]` `[NEW]` `Design/Reports/Month4/W15/W15_QA_Report.md` (map DoD + Art Direction §11 + axe summary + Pattern/Layout/Flow coverage) + `build_output.txt`. `[MODIFY]` `Design/Frontend/` trạng thái Flows/Layouts/Patterns → "implemented" + ghi "axe automated". **Đóng Phase 4.**

## W15 Locked Decisions (review-agreed)

1. **Hardening, không đổi behavior/shape.** Verify + fix tối thiểu khi gate đỏ; không feature mới/redesign/đổi CanonicalTypes (`WorkFlow.md` DoD).
2. **Axe = devDependency, Vitest+jsdom.** `axe-core` exact pin; không Playwright (deferred); axe không phải runtime.
3. **W15 gánh QA của W13/W14.** Axe automation + Visual QA toàn màn + before/after evidence + verify dark/motion/responsive dời sang W15; W13/W14 chỉ self-check theo tuần.
4. **Axe gate hội tụ ở W15.** Phase 4 axe 0 critical chốt tại đây; W12–W14 a11y là checklist thủ công.
5. **Report Output bất biến.** Chỉ Workspace UI; `--rs-report-*`/A4 deterministic.
6. **Day 4 là buffer.** Hấp thụ spillover edge-state Day 3 của W13/W14; đủ state theo `_ComponentSpecRule.md §3`.

## Risks

| Risk | Level | Mitigation |
|---|---:|---|
| "Hardening" lén đổi behavior/logic | High | Chỉ verify/fix tối thiểu; diff review; không đổi shape (Locked #1). |
| Vỡ deterministic preview ↔ PDF | High | Không đụng `--rs-report-*`/pipeline (Locked #5). |
| Axe critical nhiều, sửa tràn ngày | Medium | Ưu tiên critical; fix tối thiểu; spillover sang Day 4 buffer (Locked #6). |
| Dark mode chói (severity-bg) | Medium | Override `§2.5` bắt buộc; verify hai theme. |
| Cài axe như runtime nhầm | Low | `axe-core` = devDependency; không import vào bundle. |
| Evidence thiếu (1 theme/viewport) | Low | Bắt buộc light+dark × ≥3 viewport (C164). |
