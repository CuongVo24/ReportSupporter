# Task Brief Week 12 - Core Team

**Module target:** Toàn bộ `src/` (tích hợp + ổn định Module 1–5) — **Beta Readiness**: chạy kịch bản end-to-end một báo cáo dự án thật, validate exports đa template, polish UI states + accessibility (checklist thủ công), chuẩn bị public demo + README evidence. **Không thêm tính năng lớn** (feature freeze phần lõi Phase 1–3); đây là tuần ổn định/đánh bóng/chứng minh (RoadMap Phase 3 / W12).
**Git branch:** `feature/W12-beta-readiness`

> **Cross-cutting constraints.** All types conform to `Design/Modules/Other/CanonicalTypes.md` (single source — never re-declare a shape). `tsconfig` `strict: true`, no `any`. Route `/` stays the workspace editor. ≤200 lines/file (`VibeCode.md` §3); inter-module imports only via each module's `index.ts`; CSS uses only `var(--rs-*)` tokens.
>
> **W12 install matrix.** **KHÔNG cài lib mới** — tuần ổn định, không feature mới. Playwright E2E vẫn **Deferred** (`TechnicalStack §7`) → verify **thủ công** + scenario script. **Accessibility tuần này = checklist thủ công** (keyboard/focus/contrast/aria); **axe tự động hoá để Phase 4 / W15** (không cài axe-core tuần này).
>
> **🔒 Feature freeze phần lõi.** Không thêm tính năng nghiệp vụ mới; chỉ localized fix khi E2E/validation lộ lỗi thật. Đo theo `ProductPRD.md §7` Success Criteria.
>
> **🔒 Báo cáo deterministic bất biến.** Export HTML/PDF/DOCX phải parity; không đụng `--rs-report-*`/pipeline trừ khi validation lộ lệch (localized fix có kiểm soát).
>
> **🔒 First screen = workspace.** Kiểm `/` là workspace editor (`ProductPRD.md §7`, `TechnicalStack §1`); demo chạy được **khi tắt AI** (AI W11 optional).

> **⚠️ Reconciliation — Phase 4 nối tiếp.** W12 đóng phần lõi Phase 1–3, **không** phải hết dự án: **Phase 4 (W13–W15)** đầu tư frontend nối tiếp (`MasterRoadMap §Phase 4`). "Feature freeze" áp cho nghiệp vụ lõi, không cấm Phase 4 dựng UI primitive.
>
> **⚠️ Reconciliation — shared states là nền cho W14.** `src/components/states/` (Empty/Loading/Error/Success) dựng ở Day 3 (C130/C131) là **baseline** mà W14 sẽ **nâng cấp** bằng primitive/pattern mới — không lặp lại.
>
> **⚠️ Reconciliation — a11y checklist, axe ở W15.** Tuần này a11y bằng checklist thủ công + `a11y_checklist.md`; gate **axe 0 critical tự động** thuộc W15 (Phase 4). Không tuyên bố "axe gate" ở W12.

## Day 1: End-to-end Scenario

- `[C126]` `[NEW]` `Design/Reports/Month3/W12/e2e_scenario.md` — kịch bản trọn vòng từng bước: chọn template → metadata → skeleton → write → check → format → evidence → export 3 format → present outline/script.
- `[C127]` Chạy kịch bản thật; ghi lỗi phát hiện thành issues fix trong tuần (localized). Không đổi shape/CanonicalTypes.

## Day 2: Export Validation

- `[C128]` Validate HTML/PDF/DOCX trên ≥3 sample (software/lab/internship template W6); đối chiếu caption/page-break/cover parity.
- `[C129]` `[MODIFY]` (chỉ nếu lệch) exporter — localized fix; `[NEW]` `Design/Reports/Month3/W12/export_validation.md` (kết quả đa template).

## Day 3: UI States Polish

- `[C130]` `[NEW]` `src/components/states/` — `EmptyState`/`LoadingState`/`ErrorState`/`SuccessState` baseline shared (≤200 dòng/file), token-only. Đây là nền W14 nâng cấp (reconcile note).
- `[C131]` `[MODIFY]` panel (`EditorPanel`/`PreviewPanel`/`CheckerPanel`/`ExportPanel`/`PresentPanel`) dùng shared states nhất quán (loading/empty/error/success); `[MODIFY]` `WorkspaceLayout.tsx` đảm bảo first screen = workspace (`ProductPRD.md §7`).

## Day 4: Accessibility (checklist thủ công)

- `[C132]` `[MODIFY]` components dùng chung — keyboard nav, focus ring (token, cấm `outline:none`), `aria-label`, contrast. **Thủ công**, không axe (axe → W15).
- `[C133]` `[NEW]` `Design/Reports/Month3/W12/a11y_checklist.md` — checklist keyboard/focus/contrast/aria pass cơ bản (baseline cho W15 axe automation).

## Day 5: Public Demo & README Evidence

- `[C134]` `[NEW]` README evidence (demo workspace, ảnh chụp, export samples) trong `Design/Reports/Month3/W12/`. Demo chạy được **khi tắt AI**.
- `[C135]` `[NEW]` `Design/Reports/Month3/W12/W12_Beta_Acceptance_Report.md` (đối chiếu `ProductPRD.md §7` Success Criteria) + `build_output.txt`. Final build + lint + typecheck + Vitest gate.

## W12 Locked Decisions (review-agreed)

1. **Feature freeze lõi; localized fix.** Không tính năng nghiệp vụ mới; chỉ sửa cục bộ khi E2E/validation lộ lỗi (`ProductPRD.md §7`).
2. **Không cài lib mới.** Playwright deferred (verify thủ công); axe-core **không** cài tuần này (→ W15).
3. **A11y = checklist thủ công; axe ở W15.** Không tuyên bố "axe gate" ở W12; `a11y_checklist.md` là baseline.
4. **Báo cáo deterministic bất biến.** Export 3 format parity; không đụng `--rs-report-*`/pipeline trừ localized fix.
5. **First screen = workspace; demo no-AI.** `/` là workspace; demo chạy được khi tắt AI (W11 optional).
6. **Shared states là nền W14.** `src/components/states/` (C130/C131) baseline; W14 nâng cấp, không lặp.
7. **Phase 4 nối tiếp.** W12 đóng lõi Phase 1–3, không hết dự án; W13–W15 đầu tư frontend (`MasterRoadMap §Phase 4`).

## Risks

| Risk | Level | Mitigation |
|---|---:|---|
| Phát hiện lỗi tích hợp muộn ở tuần cuối lõi | High | Feature freeze; chỉ localized fix; ưu tiên đường E2E chính (Locked #1). |
| Export lệch trên template ít test | Medium | Validate đa template (W6) Day 2; sửa exporter có kiểm soát (Locked #4). |
| Demo phụ thuộc AI (optional) | Medium | Demo chạy được khi tắt AI; AI chỉ điểm cộng (Locked #5). |
| First screen trượt thành landing | Medium | Kiểm `/` = workspace (Locked #5). |
| Hiểu nhầm W12 là hết dự án | Low | Reconcile: Phase 4 (W13–W15) nối tiếp (Locked #7). |
| Thiếu evidence cho beta | Low | Chuẩn hoá README evidence + acceptance report (`Design/Reports/`). |
