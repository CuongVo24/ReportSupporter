# Task Brief Week 21 - Core Team

**Module target:** `src/modules/import/**` (NEW), `src/types/import.ts` (NEW), `src/modules/write/` (điểm cắm dropzone) — **Import Foundation & DOCX**: khoá Import Model (`CanonicalTypes.md` §11), dựng converter registry + universal dropzone (đường `.md` hiện có refactor thành converter đầu tiên, behavior giữ nguyên), reverse pipeline HTML→Markdown chỉ sống trong Import, và DOCX converter qua `mammoth` (RoadMap Phase 5 / W21, `Design/Modules/6.Import.md`).
**Git branch:** `feature/W21-import-foundation`

> **Cross-cutting constraints.** All types conform to `Design/Modules/Other/CanonicalTypes.md` §11 (single source — never re-declare a shape). `tsconfig` `strict: true`, no `any`. Route `/` stays the workspace editor. ≤200 lines/file (`VibeCode.md` §3); inter-module imports only via `index.ts`; CSS uses only `var(--rs-*)` tokens.
>
> **W21 install matrix.** Adds **runtime** deps (exact pin, approve trước khi cài): `rehype-remark`, `remark-stringify`, `mammoth`. **FORBIDDEN:** import `rehype-remark`/`remark-stringify`/`mammoth` ngoài `src/modules/import/` (grep gate); server-side conversion; WYSIWYG/rich-text lib; sinh mdast trực tiếp từ converter (`PipelineContract.md` §4 — Markdown là giao diện duy nhất).
>
> **🔒 Import không đổi scope Write.** Write vẫn chỉ ăn Markdown + assets; Import bàn giao qua `ImportDraft`, commit qua `appendSections`/`replaceSections` + `import-assets` hiện có — Import không tự ghi IndexedDB.
>
> **🔒 Sanitize bắt buộc.** HTML trung gian (mammoth) phải qua sanitize (cùng policy preview) trước `rehype-remark`.

> **⚠️ Reconciliation — Non-goal PRD đã amended.** `ProductPRD.md` §6 "convert every file format" gỡ một phần: 4 format curated, import-to-edit. `1.Write.md` §2 out-of-scope import đã superseded → Module 6.
>
> **⚠️ Reconciliation — `MarkdownImportDraft` absorb vào `ImportDraft`.** Không tồn tại hai shape draft song song sau W21; regression test đường `.md` cũ bắt buộc.

## Day 1: Import Types & Reverse Pipeline

- `[C166]` `[NEW]` `src/types/import.ts` + zod schema khớp CanonicalTypes §11 (`ImportSourceFormat`, `ImportWarning`, `ImportResult`, `ImportConverter`, `ImportDraft`); `[MODIFY]` `ReportIssue.module` thêm `"import"`. Export qua `src/types/index.ts`.
- `[C167]` Cài `rehype-remark` + `remark-stringify` + `mammoth` (exact pin); `[NEW]` `src/modules/import/html-to-markdown.ts` — sanitize → rehype-remark → remark-stringify (GFM), unit test các phần tử: heading/list/table/img/blockquote/code.

## Day 2: Converter Registry & Universal Dropzone

- `[C168]` `[NEW]` `src/modules/import/registry.ts` — `resolve(file)` theo extension + MIME (extension thắng khi MIME rỗng/sai), `maxBytes` gate → `file-too-large`, format lạ → reject có thông báo; `[NEW]` `converters/markdown.ts` (refactor đường `.md` hiện có thành converter, behavior y hệt).
- `[C169]` `[MODIFY]` `MarkdownImportDropzone` → `UniversalImportDropzone` — accept list sinh từ registry, mỗi file một kết quả/lỗi độc lập (fail 1 file không giết batch); regression test import `.md`.

## Day 3: DOCX Converter

- `[C170]` `[NEW]` `src/modules/import/converters/docx.ts` — mammoth → HTML → `htmlToMarkdown()`; heading/list/table sang GFM.
- `[C171]` Strip số chương hard-code đầu heading ("1.", "1.2 ") — Format là nguồn số duy nhất (`2.Format.md`); phần tử không convert được → `unsupported-element` warning, không nuốt im lặng.

## Day 4: Assets, Section Split & ImportDraft

- `[C172]` Ảnh nhúng docx → `ReportAsset` base64 qua đường `import-assets` hiện có; Markdown rewrite `asset://<id>`; ảnh hỏng/quá lớn → `image-skipped`.
- `[C173]` `[MODIFY]` `markdown-import.ts` — absorb `MarkdownImportDraft` vào `ImportDraft` (CanonicalTypes §11); heading→section split tái dùng logic readme-import; wire draft confirm hiện có (preview diff đầy đủ để W24).

## Day 5: Fixtures, Tests & QA

- `[C174]` `[NEW]` fixtures `.docx` (báo cáo VN thực tế: heading đa cấp, list, bảng, ảnh, track-changes) + snapshot tests Vitest cho docx + html-to-markdown + registry.
- `[C175]` `[NEW]` `Design/Reports/Month6/W21/W21_QA_Report.md` (đối chiếu nguồn↔kết quả, warnings, DoD map) + `build_output.txt`; grep gate reverse-pipeline-chỉ-trong-Import.

## W21 Locked Decisions (review-agreed)

1. **Markdown là giao diện duy nhất.** Converter output Markdown + assets; không sinh mdast/hast (`PipelineContract.md` §4).
2. **Reverse pipeline khoá trong Import.** `rehype-remark`/`remark-stringify` không xuất hiện ngoài `src/modules/import/`.
3. **DOCX trước, PDF sau.** W21 chỉ DOCX + foundation; PDF/XLSX/PPTX/OCR là W22-24 — không làm trước.
4. **Một shape draft.** `ImportDraft` (CanonicalTypes §11) thay `MarkdownImportDraft`; đường `.md` regression-tested.
5. **Commit qua Write.** `appendSections`/`replaceSections` + `import-assets`; Import không ghi IndexedDB trực tiếp.
6. **Warning trung thực.** Mất nội dung → warning có code chuẩn (`6.Import.md` §3.2), không im lặng.

## Risks

| Risk | Level | Mitigation |
|---|---:|---|
| XSS qua HTML mammoth | High | Sanitize bắt buộc trước rehype-remark; fixture độc hại trong test (Locked #6, PipelineContract §4). |
| Reverse pipeline rò ra module khác | High | Grep gate C175; import-boundary qua `index.ts`. |
| Refactor dropzone vỡ đường `.md` | Medium | Regression test C169; behavior giữ nguyên (Locked #4). |
| Converter sinh heading mang số cứng | Medium | Strip C171; Check rule `hardcoded-heading-number` là lưới an toàn. |
| Scope leo sang PDF/preview sớm | Medium | Locked #3; out-of-scope tuần ghi rõ. |
| Bundle phình vì mammoth | Low | Dynamic import converter trong registry. |
