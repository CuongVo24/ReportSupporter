# AI Assistant uses a client-key strategy.
# Do not configure provider API keys as server-side fallbacks in this app.
# Users enter their own provider key in AI Settings; the browser stores it
# locally and sends it to /api/ai through the x-api-key header per request.
